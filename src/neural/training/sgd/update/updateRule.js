class UpdateRule {
    /**
     * @param {StochasticGradientDescent} training
     */
    init(training) {
    }

    /**
     * @param {Array} gradients
     * @param {Array} weights
     */
    update(gradients, weights) {
    }
}

module.exports = UpdateRule;